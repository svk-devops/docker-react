sudo: required
services:
    - docker

before_install:
    - docker build -t sergykozlov/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true sergykozlov/docker-react npm run test -- --coverage
